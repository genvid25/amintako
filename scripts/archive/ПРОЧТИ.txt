Архив. Здесь лежит то, что в текущей схеме (генерация в браузере + Supabase +
GitHub Pages) больше не используется, но выброшено быть не должно — история.

  generate.py, generate_openai.py — генераторы кадров через API (Gemini и OpenAI).
      Пилот с API отменён; запускать их нельзя, оставлены для истории.
  serve.py — локальный сервер дашборда (http://localhost:8787). Был нужен, пока
      дашборд открывали с диска. Сейчас дашборд живёт на GitHub Pages, а данные —
      в Supabase, поэтому локальный режим мёртв. Оставлен для истории.
  batch2.json — тестовые промты (сцена 4), проверка нового ПРОМТ-ГАЙДа. История.
  АГЕНТ.md — прежняя инструкция агента, написанная под генерацию через API.
      Вытеснена файлом ВОРКЕР.md в корне проекта. Оставлена для истории.
